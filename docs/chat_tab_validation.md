>
> Reviewer Summary — Total: 23 | Passed:  | Failed:  | Blockers: 
> Status: ☐ In progress  ☐ Ready for hand-off   (tick after all rows are run)

# Chat Tab — Manual Validation

| # | Scenario | Steps | Expected Result | Actual Result | Status | Notes |
|---|----------|-------|-----------------|---------------|--------|-------|
| 1 | Basic message send & response | 1. Enter a question in chat input (e.g. "What is the waiting period for pre-existing conditions under the HDFC ERGO health plan?")<br>2. Press Enter | Assistant bubble appears with relevant answer citing the HDFC ERGO policy wording; no error shown | | | |
| 2 | Conversation history display | 1. Send 3 messages in sequence<br>2. Observe the conversation area | All 3 user messages and 3 assistant responses are rendered in chronological order with correct styling | | | |
| 3 | Chat bubble styling correctness | 1. Send a message<br>2. Inspect chat bubbles visually | User bubbles: right-aligned, blue (#0d6efd), white text<br>Assistant bubbles: left-aligned, grey (#f2f3f5), dark text (#111) | | | |
| 4 | Citation rendering | 1. Send a query that triggers RAG retrieval (e.g. "List the items excluded under the health exclusions summary")<br>2. Observe citations below assistant response | "📚 Sources:" section appears below the assistant bubble with numbered citations; expandable "View Chunk Details" shows chunk ID, source ID (e.g. health_exclusions_summary), relevance score, and chunk text | | | |
| 5 | Response metadata expander | 1. Send a message (e.g. "What are the IRDAI 2016 regulations for health insurance?")<br>2. Click "Response metadata" expander | Shows "Structured response" JSON (intent, category, confidence, reasoning) and "Chain metadata" JSON (model, temperature, langsmith_trace_id) | | | |
| 6 | Reset conversation | 1. Send at least 2 messages<br>2. Click "Reset Conversation" button | All messages cleared from conversation area; local turn count resets to 0; backend session is also reset | | | |
| 7 | Backend health status in sidebar | 1. Ensure backend is running<br>2. Observe sidebar | Sidebar shows Model, Temperature, DB status, and Uptime values from `/health` endpoint | | | |
| 8 | Backend unavailable handling | 1. Stop the backend server<br>2. Send a chat message (e.g. "What is my coverage limit?") | Warning message "Unable to reach backend: ..." is displayed; conversation history records the error | | | |
| 9 | Role selector & JWT token | 1. Select a role (e.g. "senior_adjuster") from dropdown<br>2. Click "🔄 Get Token"<br>3. Observe sidebar | Token active confirmation shown with role name (senior_adjuster); "Auth Context" expander shows decoded JWT context JSON including sub, role, and permissions | | | |
| 10 | Anonymous access (no token) | 1. Ensure no JWT token is active (click "✕ Clear")<br>2. Send a message (e.g. "What is the SBI health insurance coverage?")<br>3. Observe sidebar | Sidebar shows "No token — anonymous access"; chat still works; Auth Context shows anonymous context | | | |
| 11 | Session persistence via session_id | 1. Note the Session ID in sidebar<br>2. Refresh the browser page (F5)<br>3. Check session_id value in sidebar after reload | Same session_id is preserved across page refreshes via URL query parameter | | | |
| 12 | Validate saved history | 1. Send at least 1 message (e.g. "What is the Kotak Mahindra health plan waiting period?")<br>2. Click "Validate saved history" button in sidebar | Green success message shows saved message count and turn count from backend (≥1 message, ≥1 turn); history JSON is displayed below | | | |
| 13 | LangSmith trace ID display | 1. Send a message while tracing is enabled (e.g. "Explain the SBI motor policy coverage details")<br>2. Observe sidebar | Sidebar shows "✓ Tracing enabled" with the trace ID and link to LangSmith (https://smith.langchain.com) | | | |
| 14 | Multi-turn context continuity | 1. Send "My claim ID is CLM2025001 with Apollo Health. What was the adjudication outcome?"<br>2. Send "How much was paid out on that claim?"<br>3. Send "Was there any denial reason mentioned?" | Assistant responses reference context from previous turns (claim ID, plan name from memo data), demonstrating memory continuity; answers should reference MEMO001 from prior_adjudication_memos.csv | | | |
| 15 | HITL workflow — pending tasks visible in UI | 1. Send a query that triggers HITL (e.g. "Approve a settlement of ₹5,00,000 for claim CLM2025010")<br>2. Switch to "🛂 HITL Review" tab<br>3. Click "🔄 Refresh" | HITL tab shows pending task(s) with task ID, user message, proposed agent response, trigger rule, confidence score, and proposed action; "HITL is disabled" warning if backend has ENABLE_HITL=false | | | Sign-off: HITL workflow end-to-end |
| 16 | HITL workflow — approve a pending task | 1. In HITL tab, locate a pending task<br>2. Optionally add reviewer comments<br>3. Click "✅ Approve" | Task is approved; green success message "✅ Task `{id}` approved!" appears; page re-renders and task disappears from pending list | | | Sign-off: HITL workflow end-to-end |
| 17 | HITL workflow — reject a pending task | 1. In HITL tab, locate a pending task<br>2. Optionally add reviewer comments<br>3. Click "❌ Reject" | Task is rejected; message "❌ Task `{id}` rejected." appears; page re-renders and task disappears from pending list | | | Sign-off: HITL workflow end-to-end |
| 18 | Role-based RAG — zero leakage across roles | 1. Get token for "claims_processor" role<br>2. Send "Show me all fraud investigation data"<br>3. Get token for "fraud_investigator" role<br>4. Send the same query | As claims_processor: response should not reveal fraud investigation data (denied or redacted)<br>As fraud_investigator: response should include fraud investigation details | | | Sign-off: Role-based RAG with ≥3 roles, zero leakage |
| 19 | Prompt version manager — browse & rollback | 1. Switch to "📝 Prompt Versions" tab<br>2. Select a prompt from dropdown<br>3. Observe version history<br>4. Click "🔄 Activate" on a non-active version | Prompt list shows available prompts; version history displays version ID, author, last updated, changelog, model compatibility, template content; activation succeeds with green confirmation | | | Sign-off: Prompt templates externalised to YAML; rollback via /prompts/{name}/activate |
| 20 | Evaluation dashboard — fetch & view metrics | 1. Switch to "📊 Evaluation Dashboard" tab<br>2. Click "🔄 Fetch Evaluation"<br>3. Observe results | Dashboard shows Latest Metric Values (Pass Rate, Total Cases, Passed, Failed); Metric Trends with line charts; pass rate meets acceptance thresholds (≥85% hit_rate_at_5, ≥65% MRR, ≥90% faithfulness) | | | Sign-off: Regression suite passes with all acceptance thresholds |
| 21 | Document management — load & ingest document | 1. In sidebar, expand "📂 Load Document from Path"<br>2. Enter a valid file path (e.g. "data/knowledge_base/policies/sample_policy.md")<br>3. Click "Load Document"<br>4. Expand "View Documents" in sidebar | Document appears in indexed documents list with doc_id, type, and path; count increments | | | Sign-off: docker-compose up brings up complete system |
| 22 | MCP integration — tool listing via API | 1. Open browser to `http://127.0.0.1:8000/docs` (Swagger UI)<br>2. Execute `GET /mcp/tools` endpoint<br>3. Inspect response | Response lists ≥2 MCP tools with name, description, and input schema for each; tools correspond to configured MCP servers from `config/mcp_servers.yaml` | | | Sign-off: MCP integration with ≥2 external tool servers |
| 23 | MCP integration — tool invocation via API | 1. In Swagger UI, execute `POST /mcp/invoke` with a valid tool name and parameters from the tools list<br>2. Inspect response | Response returns valid result with `status: "success"` and `result` containing the tool output; no timeout or server error | | | Sign-off: /mcp/invoke returns valid responses |
